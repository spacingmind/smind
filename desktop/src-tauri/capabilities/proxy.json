{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "proxy",
  "description": "IPC for the bundled UI's connection picker (AC5): list/add/remove/select a daemon connection, read the current one, and open an http(s) link in the OS browser. Scoped to the loopback proxy origin only -- the daemon/relay content served through it never gets these commands.",
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:*"]
  },
  "windows": ["main"],
  "permissions": [
    "allow-connections-list",
    "allow-connections-add",
    "allow-connections-remove",
    "allow-connections-select",
    "allow-connections-get-current",
    "allow-open-external"
  ]
}
